import styled from "@emotion/styled";

const StyledButton = styled.button`
  background: #2f2f2f;
  color: silver;
  min-width: 50px;
  padding: 5px;
  cursor: pointer;
  border: none;

  &:hover {
    color: #2f2f2f;
    background: silver;
  }
`;

export default StyledButton;
